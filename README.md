# Telegram Question Monitor

🤖 AI-powered мониторинг вопросов из Telegram чатов с интеллектуальной фильтрацией потенциальных заказов для Python разработчика.

## 📋 Описание

Система автоматически:
- ✅ Мониторит указанные Telegram чаты
- ✅ Находит реальные вопросы, отсеивая спам, рекламу и риторику
- ✅ Анализирует вопросы через AI (OpenRouter) на предмет потенциальных заказов
- ✅ Отправляет найденные вопросы в Telegram бот

## 🎯 Как работает фильтрация

### Этап 1: Regex-фильтры (базовая очистка)
Отсеивает очевидный мусор:
- ❌ Реклама и спам ("сотрудничество", "предлагаю услуги")
- ❌ Риторические вопросы ("ваше мнение", "что думаете")
- ❌ Приветствия и оффтоп ("привет", "как дела", "понедельник")
- ❌ Сообщения со ссылками
- ❌ Слишком короткие (<20 символов) или длинные (>700 символов)

**Результат:** ~20% сообщений проходят

### Этап 2: AI-анализ потенциального заказа
OpenRouter API проверяет, является ли вопрос потенциальным заказом для Python разработчика:
- ✅ Автоматизация задач (Excel, парсинг, обработка данных)
- ✅ Создание ботов (Telegram, Discord, WhatsApp)
- ✅ Интеграция сервисов (API, маркетплейсы, CRM)
- ✅ Парсинг данных (сайты, соцсети, Авито, Ozon, Wildberries)
- ✅ Веб-приложения и скрипты
- ✅ Настройка и доработка решений

**Результат:** ~40% от прошедших regex проходят AI-проверку

### Итоговая конверсия: ~8% сообщений становятся вопросами

## 🏗️ Архитектура

Проект следует SOLID-принципам и использует паттерны:
- **Repository Pattern** — абстракция работы с Telegram API
- **Service Layer** — бизнес-логика изолирована от инфраструктуры
- **Adapter Pattern** — изоляция внешних библиотек (Telethon, aiogram)
- **Factory Pattern** — создание объектов с зависимостями
- **Dependency Injection** — внедрение зависимостей через конструкторы
```
telegram_question_monitor/
├── src/
│   ├── config/          # Конфигурация и логирование
│   │   ├── settings.py  # Настройки из .env (только os.getenv)
│   │   └── logger.py    # Структурированное логирование (JSON)
│   ├── models/          # Доменные модели (dataclasses)
│   │   ├── chat.py      # Модель чата
│   │   └── question.py  # Модель вопроса
│   ├── repositories/    # Репозитории и фабрики
│   │   ├── base.py              # Базовый интерфейс
│   │   └── chat_repository.py   # Фабрика репозитория
│   ├── services/        # Бизнес-логика
│   │   ├── question_filter.py       # Regex-фильтрация
│   │   ├── ai_analyzer.py           # AI-анализ через OpenRouter
│   │   └── telegram_monitor_service.py  # Главный сервис
│   ├── adapters/        # Адаптеры внешних библиотек
│   │   ├── telegram_client.py  # Telethon адаптер (чтение чатов)
│   │   └── telegram_bot.py     # aiogram адаптер (отправка в бот)
│   └── __main__.py      # Точка входа
├── .env                 # Конфигурация (не в git!)
├── pyproject.toml       # Настройки проекта
└── requirements.txt     # Зависимости
```

## 🚀 Быстрый старт

### 1. Требования

- Python 3.11 или выше
- Telegram API credentials (получить на https://my.telegram.org/auth)
- OpenRouter API ключ (получить на https://openrouter.ai/)
- Telegram бот токен (создать через @BotFather)

### 2. Установка
```bash
# Клонировать репозиторий
git clone <your-repo-url>
cd telegram-question-monitor

# Создать виртуальное окружение
python -m venv venv

# Активировать виртуальное окружение
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Для разработки (опционально)
pip install -r requirements-dev.txt
```

### 3. Настройка
```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env и заполнить все переменные
nano .env  # или любой другой редактор
```

**Обязательные переменные в .env:**
```env
TELEGRAM_API_ID=your_api_id
TELEGRAM_API_HASH=your_api_hash
TELEGRAM_PHONE=+79160000000
TELEGRAM_BOT_TOKEN=bot_token_from_botfather
TELEGRAM_BOT_CHAT_ID=your_user_id
MONITORED_CHATS=https://t.me/chat1,https://t.me/chat2
OPENROUTER_API_KEY=sk-or-v1-your-key
```

### 4. Запуск
```bash
# Запустить мониторинг
python -m src
```

При первом запуске Telegram может запросить код подтверждения — введите его в консоль.

## ⚙️ Конфигурация

Все настройки задаются через переменные окружения в `.env`:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `TELEGRAM_API_ID` | API ID из my.telegram.org | `12345678` |
| `TELEGRAM_API_HASH` | API Hash из my.telegram.org | `abc123...` |
| `TELEGRAM_PHONE` | Номер телефона с кодом страны | `+79160000000` |
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather | `123:ABC...` |
| `TELEGRAM_BOT_CHAT_ID` | ID чата для отправки вопросов | `123456789` |
| `MONITORED_CHATS` | Чаты для мониторинга (через запятую) | `https://t.me/chat1,-1001234` |
| `OPENROUTER_API_KEY` | API ключ OpenRouter | `sk-or-v1-...` |
| `OPENROUTER_MODEL` | Модель для анализа | `google/gemini-2.0-flash-exp:free` |
| `MONITORING_INTERVAL` | Интервал проверки (секунды) | `3600` |
| `MESSAGES_LIMIT` | Кол-во сообщений для анализа | `100` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `LOG_FORMAT` | Формат логов | `json` или `console` |

## 🔍 Детали работы AI-анализатора

### Промпт для проверки потенциального заказа

AI оценивает вопросы по следующим критериям:

**✅ Является потенциальным заказом:**
- Автоматизация рутинных задач (Excel, парсинг, обработка данных, отчёты)
- Создание ботов (Telegram, Discord, WhatsApp, автоответчики)
- Интеграция сервисов (API, маркетплейсы, CRM, 1C, внешние системы)
- Парсинг данных (сайты, соцсети, маркетплейсы, Авито, Wildberries, Ozon)
- Создание веб-приложений, скриптов или утилит
- Оптимизация или ускорение работы через код
- Работа с базами данных, аналитикой данных, обработкой файлов
- Настройка или доработка существующих решений
- Вопросы "как сделать/автоматизировать/настроить [конкретную задачу]"
- Запросы помощи с технической задачей, которая решается кодом

**❌ Не является заказом:**
- Общие разговоры без конкретной задачи ("как дела?", "что нового?")
- Чисто теоретические вопросы без практического применения
- Поиск готовых сервисов БЕЗ кастомизации ("какой сервис выбрать?")
- Задачи, не требующие программирования (дизайн, маркетинг, юридические вопросы)

### Системный промпт

AI работает с установкой: *"Ты Python разработчик, который ищет возможности для сотрудничества. Оцени реалистично: может ли это быть задачей для Python разработчика? Если есть намёк на техническую задачу — отвечай 'да'."*

Это делает AI **менее строгим** и позволяет находить больше потенциальных заказов.

## 🛠️ Разработка

### Проверка кода
```bash
# Линтинг и форматирование (ruff)
ruff check src/
ruff format src/

# Проверка типов (mypy)
mypy src/

# Запуск всех проверок
pre-commit run --all-files
```

### Установка pre-commit hooks
```bash
pre-commit install
```

Теперь проверки будут запускаться автоматически перед каждым коммитом.

### Структура логов

Логи в JSON формате содержат:
- `timestamp` — время события (ISO 8601, UTC)
- `level` — уровень (DEBUG, INFO, WARNING, ERROR)
- `event` — имя события
- Дополнительные поля с контекстом

Пример:
```json
{
  "timestamp": "2026-02-11T18:30:15.123456Z",
  "level": "info",
  "event": "question_sent",
  "chat_id": -1001234567890,
  "message_id": 12345,
  "sender": "Александр"
}
```

## 📊 Пример работы
```
============================================================
  Telegram Question Monitor
============================================================

✅ Конфигурация загружена (3 чатов)
🔌 Подключение к Telegram...
✅ Подключено к Telegram

🔍 Проверка доступности чатов...

   ✅ Python автоматизация
   ✅ Фриланс чат
   ✅ Разработка ботов

✅ Доступных чатов: 3 из 3

🚀 Мониторинг запущен
⏱️  Интервал: 60 минут

============================================================
  Итерация #1 — 18:30:00
============================================================

📱 Обработка: Python автоматизация
   🔍 Найдено потенциальных вопросов: 12
   ✅ Подходящих вопросов (потенциальные заказы): 3
   📤 Отправлен вопрос от Александр
   📤 Отправлен вопрос от Мария
   📤 Отправлен вопрос от Дмитрий

📱 Обработка: Фриланс чат
   🔍 Найдено потенциальных вопросов: 8
   ✅ Подходящих вопросов (потенциальные заказы): 2
   📤 Отправлен вопрос от Анна
   📤 Отправлен вопрос от Сергей

✅ Найдено вопросов: 5

⏳ Следующая проверка в 19:30:00
```

## 📝 Принципы разработки

Проект следует:
- ✅ **SOLID-принципам** — каждый модуль имеет четкую ответственность
- ✅ **Type hints везде** — полная типизация для mypy strict mode
- ✅ **Async/await** — асинхронная обработка IO операций
- ✅ **Структурированное логирование** — JSON логи с контекстом
- ✅ **Dependency Injection** — зависимости через конструкторы
- ✅ **Graceful shutdown** — корректное завершение при Ctrl+C
- ❌ **БЕЗ Pydantic** — только dataclasses из стандартной библиотеки
- ❌ **БЕЗ сторонних валидаторов** — только os.getenv() + ручные проверки

## 🎛️ Настройка фильтрации

### Изменение строгости regex-фильтров

Отредактируйте `src/services/question_filter.py`:
- Добавьте/удалите паттерны в `SPAM_PATTERNS`, `BORING_PATTERNS` и т.д.
- Измените минимальную/максимальную длину сообщений

### Изменение строгости AI-анализа

Отредактируйте `src/services/ai_analyzer.py`:
- Измените промпт `POTENTIAL_ORDER_PROMPT_TEMPLATE`
- Добавьте/уберите критерии в списке "✅ ДА" или "❌ НЕТ"
- Измените системный промпт `POTENTIAL_ORDER_SYSTEM_PROMPT`

### Модель OpenRouter

Можно использовать другие модели через переменную `OPENROUTER_MODEL`:
- `google/gemini-2.0-flash-exp:free` (бесплатная, быстрая)
- `anthropic/claude-3.5-sonnet` (платная, качественная)
- `openai/gpt-4o` (платная, универсальная)

## 📄 Лицензия

MIT License

## 👤 Автор

Александр — Python разработчик по автоматизации

---

**Вопросы?** Создайте issue в репозитории.